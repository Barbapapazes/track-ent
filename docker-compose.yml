version: "3.8"
services: 
  backend:
    build: ./server
    ports:
      - 5000:5000
    env_file: ./server/.env
    volumes:
      - track-ent-data:/data
  front:
    build: ./client
    ports: 
      - 3000:3000
    env_file: ./client/.env
volumes:
  track-ent-data:
    name: track-ent-data